# Railway: This is a monorepo. Set Root Directory to 'backend' in Settings.
# Or Railway will use backend/requirements.txt via the nixpacks config.
fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.3
requests==2.31.0
python-dotenv==1.0.0

